{
    "php_version": "8.2",
    "build_command": "composer install --no-dev --optimize-autoloader && npm install && npm run build",
    "start_command": "php artisan migrate --force && php artisan storage:link && php artisan config:cache && php artisan route:cache && php-fpm"
}
